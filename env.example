# Flask Configuration
FLASK_ENV=development
FLASK_SECRET_KEY=

# Encryption (optional)
ENCRYPTION_KEY=
ENCRYPTION_KEY_SEED=dev-seed-change-in-production

# Exchange Logging
EXCHANGE_LOG_PATH=logs/exchange_traffic.log

# Application Settings
ADMIN_LOG_TAIL=200
STALE_WALLET_HOURS=2  # Threshold (hours) for showing wallet staleness warnings. Wallets not updated in >N hours are marked as stale.

# Apex Omni (legacy - wallet credentials now stored in database)
APEX_NETWORK=main
APEX_KEY=
APEX_SECRET=
APEX_PASSPHRASE=

# No optional filters; the script prints the entire wallet and all trades

